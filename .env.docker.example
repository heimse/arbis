# Пример переменных окружения для Docker
# Скопируйте этот файл в .env.docker и заполните значения

# Database (автоматически настроена через docker-compose)
# DATABASE_URL устанавливается автоматически, но можно переопределить
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/buildplanner?schema=public

# NextAuth
# Сгенерировать секретный ключ: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-min-32-characters-long-change-in-production

# URL приложения
# Для локальной разработки
NEXTAUTH_URL=http://localhost:3000
# Для production измените на ваш домен
# NEXTAUTH_URL=https://yourdomain.com

# Yandex Cloud API
YANDEX_CLOUD_API_KEY=your-api-key-here
YANDEX_CLOUD_PROJECT_ID=b1gqkhrllleglkvashd3
YANDEX_CLOUD_AGENT_ID=fvtj2snqf0ane0lg8rqf

